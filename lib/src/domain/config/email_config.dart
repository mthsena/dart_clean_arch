class EmailConfig {
  const EmailConfig._();

  static const symbol = '@';
  static const domain = '.com';
  static const emptyError = 'email-empty-error';
  static const formatError = 'email-format-error';
}
